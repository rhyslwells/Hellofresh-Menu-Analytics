- [ ] Set up git action so that most recent report is syncd as the repo readme.
- [ ] Set up tests for 0_setup.py and 1_bronze.py



- [x] in ./output i have output\gold_analytics.db and output\silver_data.db still do i still need these given that we will be using databricks for the storage?
DONE:
- [x] I have the API key how do i ensure that databricks can run 1_bronze.py without loading the key into the repo?